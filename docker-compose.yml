services:
  markdown-renderer:
    build: ./builder
    user: "1000:1000"
    working_dir: /app
    volumes:
      - .:/app
      - /app/builder/node_modules
    environment:
      - NODE_ENV=production
    command: ["node", "builder/build.js"]
